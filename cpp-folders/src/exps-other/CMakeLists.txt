# Exps other — shared/tooling + legacy single-header renderer.
# Location: cpp-folders/src/exps-other/CMakeLists.txt
# hello-shs-renderer first: defines HelloSHSRenderer_SOURCE_DIR used by SW exps.

add_subdirectory("hello-shs-renderer")
add_subdirectory("hello-parallelization")
add_subdirectory("hello-other-exps")
