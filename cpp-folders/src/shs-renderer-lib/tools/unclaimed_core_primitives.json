{
  "note": "ROP-1.3 (owner ruling 2026-09-18): a core/ primitive may be unclaimed ONLY with a recorded disposition. The gate prints every entry as KNOWN on each run, so this debt stays visible instead of silently passing. Never add an entry to silence a newly unclaimed primitive — that silence is the exact rot that retired core/result.hpp. Each entry needs an owner decision; the two OPEN DECISION entries below are recorded findings, not approvals.",
  "entries": [
    {
      "header": "shs/core/context.hpp",
      "disposition": "PENDING PROPOSAL, not rot — the app-context proposal recorded in docs/backlog/engine_header_inventory.json moves this header to shs/app/context.hpp with reason 'split-values-from-runtime-before-move'. The move owns the header, so no consumer is expected until it lands."
    },
    {
      "header": "shs/core/log.hpp",
      "disposition": "OPEN DECISION (finding ROP-1.4, measured 2026-09-18): zero consumers anywhere in the repo and no gate test — the same unclaimed-primitive class as the retired core/result.hpp. It is coherent rather than contradictory, so it was listed instead of deleted. Owner to decide: give it a consumer, cover it with a gate, or delete it."
    },
    {
      "header": "shs/core/time.hpp",
      "disposition": "OPEN DECISION (finding ROP-1.4, measured 2026-09-18): zero consumers anywhere in the repo and no gate test — the same unclaimed-primitive class as the retired core/result.hpp. FrameClock is coherent rather than contradictory, so it was listed instead of deleted. Owner to decide: give it a consumer, cover it with a gate, or delete it."
    }
  ]
}
