#pragma once

// snake event: discrete outcomes emitted by the reducer each tick.
// Re-exports the shared vocabulary from snake.contract.hpp (SnakeEventType, SnakeEvent) so any pod
// can include this file without duplicating definitions — DOD reusable-vocabulary test.
#include "snake.contract.hpp"
